name: farmer_app
description: "Farmer Dashboard & Field Monitoring App"
publish_to: 'none'
version: 1.0.0+1

# ---> ADD THIS ENVIRONMENT BLOCK <---
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  http: ^1.2.0
  geolocator: ^11.0.0
  image_picker: ^1.1.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  sensors_plus: ^5.0.0
  camera: ^0.10.5+9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true